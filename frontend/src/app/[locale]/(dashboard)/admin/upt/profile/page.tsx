"use client";

import CampusInfoPage from "../../campus/info/page";

// UPT Org Profile reuse the same logic as Campus Info but for their own org
export default function UptProfilePage() {
    return <CampusInfoPage />;
}
